

#Create an ifelse() function that does the following: 
#if the component is greater than zero it writes "OK", otherwise it writes "Wrong".


x <- c(10,-5,11,9,-3.55)
x


y <- ifelse(x>0, "OK", "Wrong")
y


